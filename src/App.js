import React from 'react';
import ReactDOM from 'react-dom';

const newElement = (
  <div>
    Новый элемент для нового приложения
    <a href="#">Ссылочка, которая пока не работает</a>
  </div>
);

export default newElement;
